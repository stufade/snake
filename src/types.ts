export enum Ceil {
  Empty,
  Body,
  Head,
  Apple
}

export enum Direction {
  Up,
  Down,
  Left,
  Right,
  Null
}

export enum Arrow {
  Up = 'ArrowUp',
  Down = 'ArrowDown',
  Left = 'ArrowLeft',
  Right = 'ArrowRight',
}
